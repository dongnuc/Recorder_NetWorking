<Window x:Class="WpfUI.Dialogs.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUI"
        mc:Ignorable="d"
        Title="Configuration" 
        Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F0F0">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="⚙️ Project Configuration" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <GroupBox Grid.Row="1" Header="Client Executable" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtClientPath" Grid.Column="0" Height="30" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="📁 Browse" Width="100" Height="30" Click="BtnBrowseClient_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Server Executable" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtServerPath" Grid.Column="0" Height="30" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="📁 Browse" Width="100" Height="30" Click="BtnBrowseServer_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Protocol" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="RbHttp" Content="🌐 HTTP" IsChecked="True" Margin="10,0,20,0" VerticalAlignment="Center"/>
                <RadioButton x:Name="RbTcp" Content="🔌 TCP" VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="BtnSave" Content="Save" Width="100" Height="40" Background="#27AE60" Foreground="White" Click="BtnSave_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="100" Height="40" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>

    </Grid>
</Window>