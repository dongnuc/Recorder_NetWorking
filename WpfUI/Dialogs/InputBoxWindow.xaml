<Window x:Class="WpfUI.InputBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Test Case" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="TxtPrompt" Text="Please enter a name for the new test case:" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="TxtInput" VerticalAlignment="Center" Height="30" KeyDown="TxtInput_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOk" Content="OK" Width="80" Height="30" IsDefault="True" Click="BtnOk_Click"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Height="30" IsCancel="True" Margin="10,0,0,0" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>